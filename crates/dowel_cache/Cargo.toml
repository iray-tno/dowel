[package]
name = "dowel_cache"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Build cache for Dowel's candidate-class scan, with a swappable backing store."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
