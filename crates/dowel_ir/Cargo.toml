[package]
name = "dowel_ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform-independent intermediate representation shared across the Dowel compiler pipeline."

[dependencies]
