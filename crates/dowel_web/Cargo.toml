[package]
name = "dowel_web"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Dowel IR to DOM/CSS/ARIA lowering (Web backend)."

[dependencies]
dowel_ir = { path = "../dowel_ir" }
# Not just for tests: the fallback path emits CSS for candidate classes
# found by `dowel_parser::scan_class_candidates`.
dowel_parser = { path = "../dowel_parser" }
