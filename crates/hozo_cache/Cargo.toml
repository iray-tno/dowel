[package]
name = "hozo_cache"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Build cache for Hozo's candidate-class scan, with a swappable backing store."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
